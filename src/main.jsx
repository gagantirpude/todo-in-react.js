import React from "react";
import ReactDOM from "react-dom/client";
import CreateContext from "./utils/CreateContext.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CreateContext />
  </React.StrictMode>
);
